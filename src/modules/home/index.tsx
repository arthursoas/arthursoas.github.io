import React, { Component } from 'react';
import Button from 'react-bootstrap/Button';

export default class Home extends Component {
  render() {
    return (
      <div>
        <p className="f1">Hello, I'm working on that (not in this moment)</p>
        <Button>Teste</Button>
      </div>
    );
  }
}